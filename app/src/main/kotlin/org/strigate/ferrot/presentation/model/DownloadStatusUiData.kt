package org.strigate.ferrot.presentation.model

enum class DownloadStatusUiData {
    QUEUED,
    WAITING_FOR_NETWORK,
    WAITING_FOR_WIFI,
    METADATA,
    DOWNLOADING,
    PAUSED,
    COMPLETED,
    FAILED,
    STOPPED,
}
